#!/bin/sh

chosen=$(printf "\n\n\n" | rofi -dmenu -i -theme-str '@import "powermenu.rasi"')

case "$chosen" in
  "") poweroff ;;
    "") reboot ;;
    "") i3lock ;;
#    "") systemctl suspend ;;
    *) exit 1 ;;
esac
