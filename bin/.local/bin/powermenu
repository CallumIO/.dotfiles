#!/bin/sh

chosen=$(printf "\n\n\n" | rofi -dmenu -i -theme-str '@import "powermenu.rasi"')

case "$chosen" in
  "") poweroff ;;
    "") reboot ;;
    "") i3lock ;;
    "") i3lock && echo mem | sudo tee /sys/power/state ;;
    *) exit 1 ;;
esac
